import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ListarproductoxcorreoPage } from './listarproductoxcorreo.page';

describe('ListarproductoxcorreoPage', () => {
  let component: ListarproductoxcorreoPage;
  let fixture: ComponentFixture<ListarproductoxcorreoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ListarproductoxcorreoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
